server:
  port: ${PRODUCT_SERVICE_PORT:8082}

spring:
  application:
    name: PRODUCT-SERVICE

  data:
    mongodb:
      uri: ${DB_URL}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}